-- Layout settings (from conf/layouts/default.conf)

hl.config({
  dwindle = {
    preserve_split = true,
  },
  master = {
    -- new_status = "master"  -- commented out per original
  },
  binds = {
    pass_mouse_when_bound = false,
  },
})
